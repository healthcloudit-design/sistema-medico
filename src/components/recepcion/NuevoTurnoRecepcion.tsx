// Movido a components/shared/NuevoTurno.tsx (ahora también lo usa el dashboard de
// profesional, con un profesional fijo en vez del paso de selección). Este archivo
// se mantiene como re-export por compatibilidad; no lo dupliques, editá el original.
export { NuevoTurno as NuevoTurnoRecepcion } from '../shared/NuevoTurno'
