node_modules/
dist/
.env
.env.local

# Scripts de migración ad-hoc (nunca deben commitearse — pueden contener tokens)
*.mjs
migrate*.js
fix-migration*.js

# Cajón de descarte local (no versionar)
_to_delete/
